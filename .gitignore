# Build output
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
artifacts/

# Self-contained single-file publish output - ~150 MB of runtime, rebuilt on demand
publish/

# Inno Setup output when the .iss is compiled from the IDE, which ignores OutputDir
installer/Output/

# Visual Studio / Rider / VS Code
.vs/
.idea/
*.user
*.suo

# Local SQLite database (lives in %LOCALAPPDATA%, but never commit a stray copy)
*.db
*.db-shm
*.db-wal

# Test results
[Tt]est[Rr]esult*/
*.trx

# Test run artifacts
TestResults/
*.cobertura.xml
